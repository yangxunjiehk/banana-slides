# AI Provider 格式配置
# "gemini" (默认): 使用 Google GenAI SDK
# "openai": 使用 OpenAI SDK 格式
AI_PROVIDER_FORMAT=gemini

# Gemini 格式配置（当 AI_PROVIDER_FORMAT=gemini 时使用）
GOOGLE_API_KEY=your-api-key-here
# 代理示例: https://aihubmix.com/gemini
GOOGLE_API_BASE=https://generativelanguage.googleapis.com
# GenAI (Gemini) 超时时间（秒），默认300秒
GENAI_TIMEOUT=300.0
# GenAI (Gemini) 最大重试次数（应用层实现），默认2次
GENAI_MAX_RETRIES=2

# OpenAI 格式配置（当 AI_PROVIDER_FORMAT=openai 时使用）
OPENAI_API_KEY=your-api-key-here
# 代理示例: https://aihubmix.com/v1
OPENAI_API_BASE=https://api.openai.com/v1
# 超时时间（秒），默认300秒
OPENAI_TIMEOUT=300.0
# 最多重试次数，减少重试避免累积超时，默认2次
OPENAI_MAX_RETRIES=2

# AI 模型配置
TEXT_MODEL=gemini-3-flash-preview
IMAGE_MODEL=gemini-3-pro-image-preview

# Flask 配置
LOG_LEVEL=INFO
FLASK_ENV=production
SECRET_KEY=your-secret-key-change-this-in-production
BACKEND_PORT=5000

# CORS 配置（多个地址用逗号分隔）
CORS_ORIGINS=*

# 并发配置
MAX_DESCRIPTION_WORKERS=5
MAX_IMAGE_WORKERS=8

# MinerU 文件解析服务配置
# 建议改成自己申请的api token以避免用量限制
MINERU_TOKEN=eyJ0eXBlIjoiSldUIiwiYWxnIjoiSFM1MTIifQ..oDW3w29YeiLLyL6x5JDKo2ZmZf5VRvcH4BpNG-mKR-Jyp6AxuGiLtiDohlD6WYLwJb7fyE5X1bgbZQ75ZwDthw
MINERU_API_BASE=https://mineru.net

# 图片识别模型配置（用于为解析文件中的图片生成描述）
IMAGE_CAPTION_MODEL=gemini-3-flash-preview

# 可编辑导出服务配置
BAIDU_OCR_API_KEY=you-baidu-api-key

# 输出语言配置
# 可选值: 'zh' (中文), 'ja' (日本語), 'en' (English), 'auto' (自动)
OUTPUT_LANGUAGE=zh

# ===== Supabase Authentication (可选) =====
# 不配置则认证功能禁用，所有用户可直接访问
# 配置后启用多用户认证和数据隔离

# Supabase 项目 URL
SUPABASE_URL=https://your-project.supabase.co
# Supabase anon (public) key - 前端使用
SUPABASE_ANON_KEY=your-anon-key
# Supabase service_role key - 后端使用（勿泄露）
SUPABASE_SERVICE_KEY=your-service-role-key
# Supabase JWT secret - 用于验证 JWT token（在 Supabase Dashboard -> Settings -> API -> JWT Secret）
SUPABASE_JWT_SECRET=your-jwt-secret

# 前端 Supabase 配置（Vite 构建时注入）
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# 管理员邮箱（可管理白名单的用户）
# 格式: 逗号分隔的邮箱列表
ADMIN_EMAILS=admin@example.com

# 邮箱白名单（初始白名单，之后通过管理界面管理）
# 格式: 逗号分隔的邮箱列表
# 不配置则允许所有已登录用户访问
ALLOWED_EMAILS=

# --- 镜像源配置（国内用户如遇网络问题，取消以下注释即可使用国内镜像源）---

# #  Docker Hub 镜像源（注意末尾斜杠）
# DOCKER_REGISTRY=docker.1ms.run/

# # GitHub Container Registry 镜像源（注意末尾斜杠）
# GHCR_REGISTRY=ghcr.nju.edu.cn/

# # Debian apt 镜像源
# APT_MIRROR=mirrors.aliyun.com

# # PyPI 镜像源
# PYPI_INDEX_URL=https://mirrors.cloud.tencent.com/pypi/simple

# # npm 镜像源（注意末尾斜杠）
# NPM_REGISTRY=https://registry.npmmirror.com/
