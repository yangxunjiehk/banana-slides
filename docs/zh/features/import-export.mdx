---
title: "导入导出格式"
description: "大纲和描述的 Markdown 导入导出格式说明"
---

Banana Slides 使用 Markdown 文件（`.md` / `.markdown` / `.txt`）导入导出大纲和页面描述，格式可读性强，可用任意文本编辑器编辑。

## 格式结构

```markdown
# 项目标题

> 生成时间: 2025/1/1 12:00:00

---

## 第 1 页: 封面

> 章节: 引言

这一页作为封面，只展示演示主题、公司名称和日期。

**页面描述：**
--- 页面文字 ---

### 项目年度汇报

公司名称

2026 年 4 月

--- 页面文字结束 ---

版式与重点：居中大标题，副标题和日期置于下方；标题为视觉重点

---

## 第 2 页: 市场概况

> 章节: 分析

这一页概述行业增长趋势、主要细分市场和关键机会。

**页面描述：**
--- 页面文字 ---

### 市场概况

- 行业规模持续增长
- 企业级需求增速更快
- 细分市场仍存在空白机会

--- 页面文字结束 ---

配图与素材：增长柱状图、关键数据卡片、市场细分示意图

---
```

## 字段说明

| 字段 | 语法 | 必填 |
|------|------|------|
| 页面标题 | `## 第 N 页: 标题` 或 `## Page N: Title` | 是 |
| 章节 | `> 章节: 名称` 或 `> Chapter: Name` | 否 |
| 大纲概述 | 页面标题后的一句普通文本 | 否 |
| 页面描述 | `**页面描述：**` 或 `**Page Description:**` 后使用 `--- 页面文字 ---` / `--- 页面文字结束 ---` 包裹页面正文 | 否 |

如果描述末尾包含形如 `配图与素材：...`、`版式与重点：...` 的额外字段，导入时也会一并保留（旧项目的 `视觉元素：...`、`排版布局：...` 同样兼容）。

页面之间用 `---` 分隔。

## 导出选项

可从大纲编辑器和详细编辑器导出：

| 来源 | 内容 |
|------|------|
| 大纲编辑器 | 页面标题和大纲内容 |
| 详细编辑器 | 可导出「仅描述」或「大纲 + 页面描述」 |

## 导入行为

- 粘贴或上传 Markdown 后，导入弹窗会先显示可识别的页面数量；只有识别到页面时才能执行导入。
- 导入的页面会按 Markdown 文件顺序**一次性追加**到现有项目，不会替换已有页面。
- 当前推荐格式是在页面标题后直接写一句大纲概述；旧的 `**大纲要点：**` / `- 要点` 结构仍可导入。
- 同时兼容中英文标记：`第 N 页` / `Page N`、`章节` / `Chapter`、`大纲要点` / `Outline Points`、`页面描述` / `Page Description`。
- 导入内容中的 HTML 标签会被自动清除。
- 页面描述末尾的短字段行（如 `版式与重点：...`）会被识别为额外字段并保留。
- 支持 `.md`、`.markdown` 和 `.txt` 文件；PDF、Word、图片等非文本文件会在读取前被拦截，并提示更换文件格式。

## 最简示例

一个有效的导入文件可以很简单：

```markdown
## 第 1 页: 开场介绍

这一页介绍演示主题、议程和演讲者。

## 第 2 页: 总结

这一页回顾核心结论和后续行动。
```

## 用外部 AI 生成导入文件

如果你想先在外部 AI 助手中批量整理大纲，或让它按固定结构产出可导入文本，可以使用下面的模板。

将提示词复制到任意 AI 助手（如 ChatGPT、Claude、Kimi），替换占位符后，让它只输出 Markdown 结果，再导入 Banana Slides。

<Tip>
  这只是给外部 AI 生成导入文件的辅助方式，不是 Banana Slides 的主要创作入口。多数情况下，直接在应用内创建项目并使用内置 AI 生成会更方便。
</Tip>

<Tip>
  这里的“页面描述”不是一句简短的画面说明，而是该页的主要文字内容与设计要求的组合。通常做法是：
  先在 `**页面描述：**` 下写该页会直接展示的 Markdown 文本（会逐字渲染到幻灯片上），再按需在末尾补充 `配图与素材：...`、`版式与重点：...`、`演讲者备注：...` 这类字段。
</Tip>

<Tip>
  如果只需要大纲（不含描述），从提示词中删除 `**页面描述：**` 相关要求即可。若想输出英文标记，也可将 `第 N 页 / 章节 / 大纲要点 / 页面描述` 换成 `Page N / Chapter / Outline Points / Page Description`。
</Tip>

<Tip>
  当前 Banana Slides 内置 prompt 更接近下面这种结构：
  页面标题之后直接给一句简短概述，不再单独生成 `**大纲要点：**`；而在 `**页面描述：**` 内部，`--- 页面文字 ---` / `--- 页面文字结束 ---` 现在应视为必需结构，而不是可选写法。
</Tip>

<Tip>
  直接导入 Markdown 时，系统仍兼容旧的 `**大纲要点：**` / `- 要点` 结构；新内容建议使用标题后的简短概述句。
</Tip>

贴近当前内置 prompt 的示例页更接近下面这种写法：

```markdown
## 第 3 页: 市场机会

> 章节: 行业分析

这一页说明市场规模增长、竞争格局分散，以及企业级机会正在放大。

**页面描述：**
--- 页面文字 ---

### 市场机会正在快速放大

- 全球市场规模预计在 2028 年达到 **320 亿美元**
- 企业级场景增速高于消费级场景
- 当前竞争格局分散，头部集中度仍然较低

| 维度 | 当前现状 | 机会判断 |
| --- | --- | --- |
| 市场规模 | 高增长 | 值得进入 |
| 竞争强度 | 中等 | 可差异化 |
| 付费意愿 | 明确 | 可快速验证 |

--- 页面文字结束 ---

配图与素材：增长曲线、对比数据卡片、行业细分示意图
版式与重点：上方标题，左侧要点，右侧数据卡片，下方表格；320 亿美元为视觉重点
```

<Accordion title="AI 提示词模板">

```text
按照如下要求生成可直接导入banana-slides的演示文稿文字描述：

对于每一页：
1. 第一行“##” 二级标题开头，后面跟页面标题
2. 第二行“>” 开头，后面根具体的章节（如果需要分章节）
3. **大纲要点：**开头，后面新开一行开始写大纲内容，阐述该页的目的和内容梗概。
4. **页面描述：**开头，后面新开一行开始写页面描述内容，包含页面文字（带上明确分割记号，切不包含任何额外解释。该字段的每一个字都会渲染到页面上，请谨慎）、页面图片等字段。
5. 如果需要，后面可以跟额外字段。
5. 用 --- 分隔不同页。

输出格式遵循以下实际范例：
<example>
---

## 第 2 页: 公司诞生前的行业环境

> 章节: Part 1: 开场与背景

**大纲要点：**
- 概述20世纪90年代初个人电脑、图形计算和半导体行业的发展背景，说明英伟达成立的市场土壤。

**页面描述：**
--- 页面文字 ---
xxx
--- 页面文字结束 ---

版式与重点：左侧要点纵向排列，右侧留行业背景示意图区
演讲者备注：强调英伟达并非凭空出现，而是诞生于PC、图形计算和半导体共同发展的时代节点
配图与素材：PC、芯片、电路纹理背景元素

---

## 第 3 页: 创始团队与公司成立

> 章节: Part 1: 开场与背景

**大纲要点：**
- 介绍黄仁勋及联合创始人的背景、创业动机、公司创立时间与初期定位，交代英伟达的起点。

**页面描述：**
--- 页面文字 ---
xxx
--- 页面文字结束 ---

版式与重点：上方放成立信息，下方分点展示团队与定位；成立信息为视觉重点
演讲者备注：介绍创始团队来自芯片与图形领域，英伟达从一开始就带有强烈技术创业色彩
配图与素材：创始团队人物感、创业时间轴元素

---
</example>


只输出 Markdown，不要额外解释。
```

</Accordion>
