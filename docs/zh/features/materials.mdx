---
title: "素材与文件"
description: "上传参考文件、粘贴图片，或用 AI 生成专属素材"
---

## 上传参考文件

上传文档后，AI 在生成大纲、描述和图片时会参考其中的内容。

如果文档解析结果中包含图片，这些图片会在参考文件关联到项目后自动进入该项目的素材库，后续编辑页面描述或替换参考图时可直接从素材库选择。

### 支持的格式

| 格式 | 提取内容 |
|------|---------|
| PDF | 文本 + 嵌入图片 |
| DOCX / DOC | 文档内容和结构 |
| PPTX / PPT | 幻灯片内容 |
| XLSX / XLS / CSV | 表格数据 |
| Markdown / TXT | 纯文本 |

<Tip>
  PDF 解析使用 [MinerU](https://mineru.net) 提供高质量内容提取。配置 MinerU Token 后效果更好，详见[配置说明](/zh/configuration#mineru-pdf-解析)。
</Tip>

### 上传方式

- 点击输入区域旁的**回形针图标**，打开文件选择器
- 直接**拖拽文件**到输入区域
- **粘贴**（Ctrl+V）文件或图片
- 点击「**素材中心**」从历史上传的文件中选择

文件大小上限：**200MB**。上传后自动开始解析，解析期间显示「解析中」状态。

### 在哪里上传

| 位置 | 说明 |
|------|------|
| 首页 | 创建项目前上传，作为整个项目的参考素材 |
| 大纲编辑器 | 左侧面板可管理已上传的文件 |
| 描述编辑器 | 顶部区域可查看和管理参考文件 |
| 幻灯片预览（单页编辑） | 可为单独一页指定参考图片 |

## 粘贴图片

在首页、大纲编辑器或描述卡片中，直接粘贴图片（Ctrl+V）：

1. 复制图片（截图、从网页复制等）
2. 在输入框中按 **Ctrl+V**
3. 图片自动上传，AI 识别内容后以 Markdown 格式插入到光标位置

识别出的图片内容会作为上下文参考。

当页面同时使用图片模板和描述中的素材图片时，支持多图输入的生图模型会同时收到模板图与素材图，不再只保留第一张参考图。GPT Image 系列最多支持 16 张输入图片（包含模板图）；DALL-E 2 仍只支持一张输入图片。

## 风格参考图

上传图片作为视觉风格参考，AI 会匹配其配色、布局和设计语言。

在首页「选择风格模板」区域或单页编辑的「上传图片」中上传参考图。

## 素材工具箱

点击顶部导航栏的「**素材生成**」后，现在会打开统一的**素材工具箱**。它不只负责文生图，还可以把现有图片当作处理目标，做局部编辑和智能擦除。

### 生成新素材

适合从提示词直接生成一张新图：

1. 输入提示词，例如：`蓝紫渐变背景，带几何图形和科技感线条`
2. 选择画面比例
3. 可选上传主参考图和额外参考图
4. 点击「**执行工具**」

### 整图编辑

适合对已有图片整体重绘、换风格、改色调：

1. 先选择一张「处理目标图」
2. 输入编辑指令，例如：`把整体改成银色金属质感，保留主体构图`
3. 可选再添加参考图
4. 点击「**执行工具**」

### 框选编辑

适合只改图片中的一部分，例如按钮、标题区、人物手上的物件：

1. 选择「**框选编辑**」
2. 上传或从素材库选择一张处理目标图
3. 点击「**开始框选**」，在图片上拖出要修改的区域
4. 输入编辑指令
5. 选择结果应用方式：
   - **只把选区覆盖回原图**：尽量保持未选区不变，只替换框内结果
   - **直接用整张结果覆盖**：仍以选区为主要编辑目标，但最终采用模型返回的整张图
6. 点击「**执行工具**」

### 智能擦除

适合移除 logo、水印、杂物、局部元素：

1. 选择「**智能擦除**」
2. 上传一张处理目标图
3. 框选要移除的区域
4. 可选补充背景要求，例如：`补成浅灰色石材纹理`
5. 点击「**执行工具**」

### 连续处理

处理完成后，可点击「**将结果作为新源图继续处理**」，把刚刚的结果直接送回工具箱继续做下一步编辑。这样适合连续完成：

- 先擦除，再局部重绘
- 先整图换风格，再精修局部
- 先生成素材，再继续迭代细节

所有处理结果都会作为**新素材**保存到素材库，原图不会被覆盖。

## 素材中心

点击顶部导航栏的「**素材中心**」，查看和管理所有历史素材：

- **筛选**：按项目筛选，或查看全部/未关联项目的素材
- **预览**：点击眼睛图标全屏预览
- **下载**：选中后点击下载，多个文件自动打包为 ZIP
- **删除**：单个删除或批量删除
- **上传**：直接在素材中心上传新图片
